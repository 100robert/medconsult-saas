// ============================================
// JEST SETUP - CONFIGURACIÓN GLOBAL
// ============================================

// Aumentar timeout para tests
jest.setTimeout(10000);

// Silenciar console en tests (descomenta si lo necesitas)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
// };
